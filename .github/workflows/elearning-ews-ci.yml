name: eLearning - EWS CI

on:
  push:
    paths:
      - "eLearning/ews/**"
      - ".github/workflows/elearning-ews-ci.yml"
  pull_request:
    paths:
      - "eLearning/ews/**"
      - ".github/workflows/elearning-ews-ci.yml"
  workflow_dispatch:

defaults:
  run:
    working-directory: eLearning/ews

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: eLearning/ews/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run unit tests
        run: python -m unittest discover -s tests -p "test_*.py" -t . -v
